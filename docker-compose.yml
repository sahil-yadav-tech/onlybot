services:

  backend:
    image: sahildocker9082/chat-backend:latest

    container_name: chat-backend

    restart: unless-stopped

    env_file:
      - /home/ubuntu/.env

    expose:
      - "1111"

  nginx:
    image: nginx:1.27-alpine

    container_name: nginx

    restart: unless-stopped

    depends_on:
      - backend

    ports:
      - "80:80"

    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro